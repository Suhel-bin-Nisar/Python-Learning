name = "suhel"

print(len(name))

print(name.endswith("el"))  # it will check that the given string "name" is ended with the given word like "l" or note. 

print(name.startswith("Su")) # same as endswith but it check the starting point and these functions are case sensitive.

print(name.capitalize())